{
  "buildCommand": "npm run build",
  "outputDirectory": "public",
  "rewrites": [
    // Let serverless functions under /api handle requests. Keep SPA fallback for client routing.
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
